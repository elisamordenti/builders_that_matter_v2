from app import app

# This is the entry point for Vercel serverless functions
# Vercel's @vercel/python builder automatically detects and serves WSGI applications
# when they're exposed directly as a variable named 'app'

